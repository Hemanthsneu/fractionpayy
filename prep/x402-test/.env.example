# x402 prototype env — copy to .env and fill in.
# TESTNET-ONLY wallets. Never put real funds on these keys.

# Wallet that RECEIVES agent fees (later: the Privy agent wallet)
AGENT_WALLET_ADDRESS=0x...

# Wallet that PAYS (FractionPay's buyer side).
# Needs: Base Sepolia USDC (https://faucet.circle.com) + a bit of Base Sepolia ETH
BUYER_PRIVATE_KEY=0x...

PORT=4021
